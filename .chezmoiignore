{{/* Files to ignore when inside a Distrobox container */}}
{{- if env "CONTAINER_ID" }}
Distrobox
Vault
.ssh

.config/gtk-3.0
.config/1Password
.config/git
{{- end }}

{{/* Files to ignore when inside a Distrobox host */}}
{{- if and (not (env "CONTAINER_ID")) (lookPath "distrobox") }}
{{- end }}
